services:
  authelia:
    extends:
      file: ../../compose.yaml.d/compose.yaml
      service: common
    environment:
      LDAP_BASE_DN: "dc=${DOMAIN_NAME},dc=${TLD}"
      OIDC_SECRET_HASH_GRAFANA: "$development_OIDC_SECRET_HASH_GRAFANA"
    depends_on:
      - lldap
